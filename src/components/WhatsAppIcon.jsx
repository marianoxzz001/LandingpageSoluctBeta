// Unmodified official Meta glyph. Provenance: public/WHATSAPP-ASSET.md.
export default function WhatsAppIcon({ size = 20, variant = 'white' }) {
  return <img src={`/whatsapp-${variant}.svg`} width={size} height={size} alt="" aria-hidden="true" className="whatsapp-icon" />;
}
